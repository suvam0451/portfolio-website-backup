---
path: 'ue4/tutorials/third-party-static-and-dynamic-libraries'
date: '2019-10-02'
title: 'Third party DLL/LIBs'
tags: ['markdown', 'occuring', 'strange']
draft: false
---

- Hello there.
- Sample content.

```javascript{1-2,22}{numberLines: true}
import React from 'react';

const Counter = initialCounter => {
  const [counter, setCounter] = React.useState(initialCounter);

  const onIncrement = () => {
    setCounter(c => c + 1);
  };

  const onIncrement = () => {
    setCounter(c => c - 1);
  };

  return (
    <div>
      {counter}

      <div>
        <button onClick={onIncrement} type="button">
          Increment
        </button>
        <button onClick={onDecrement} type="button"> // highlight-line
          Decrement
        </button>
      </div>
    </div>
  );
};

export default Counter;
```